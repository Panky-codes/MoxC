name: C/C++ CI

on: [push, pull_request]

jobs:
  build-ubuntu:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Install dependencies
      run: |
           pwd
           sudo apt-get install -y clang libclang-dev
           export CC="gcc"
           export CXX="g++"
           pip3 install wheel setuptools
           pip3 install conan
           export PATH=$PATH:/home/runner/.local/bin
           echo $PATH
           conan --version
           ls /usr/lib/llvm*/lib/libclang*
           ls /usr/lib/llvm*/include/clang*
           
           sudo ln -s ~/.local/bin/conan /usr/local/bin/conan
           sudo ln -s /usr/lib/llvm-6.0/lib/libclang-6.0.so /usr/lib/
           sudo ln -s /usr/lib/llvm-6.0/include/clang-c/ /usr/include/
    - name: configure
      run: mkdir build && cd build && cmake -DENABLE_TESTING=ON ..
    - name: build
      run: cmake --build build
    - name: test
      run: cd build && ctest
